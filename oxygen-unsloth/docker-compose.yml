services:
  app_proxy:
    environment:
      APP_HOST: oxygen-unsloth_web_1
      APP_PORT: 8888
      PROXY_AUTH_ADD: "false"

  web:
    image: unsloth/unsloth:latest
    restart: on-failure
    ipc: host
    environment:
      - PORT=8888
    volumes:
      - ${APP_DATA_DIR}/data:/workspace/work
